{
  "name": "blog_list",
  "version": "1.0.0",
  "description": "blog_list fullstack course ahercoa",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint ."
  },
  "author": "Antonio Hernández Coarasa",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "eslint": "^9.10.0",
    "express": "^4.19.2",
    "express-async-errors": "^3.1.1",
    "lint": "^1.1.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.5.1",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  }
}
